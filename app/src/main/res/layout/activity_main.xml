<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/lytCord"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.activity.MainActivity">

<!--    Try to use dimens.xml instead of static attrs, like elevation="16dp"    -->
    <com.ismaeldivita.chipnavigation.ChipNavigationBar
        android:id="@+id/bottomMenu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:background="@android:color/white"
        android:elevation="16dp"
        android:minHeight="56dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        app:cnb_addBottomInset="true"
        app:cnb_menuResource="@menu/menu"
        app:cnb_orientationMode="horizontal" />

    <FrameLayout
        android:id="@+id/frgMain"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:visibility="invisible" />

    <!-- Do not afraid of using long component names.
    This should be something like; mainLoadingProgressBar
    Since we started to use synth import, it is hard to understand which component it is.
    Also you can make it shorter in import section.
    For example when you import this component in kt file you can change its name.
    I wrote a comment in MainActivity import section.-->
    <ProgressBar
        android:id="@+id/prgBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center" />

    <include layout="@layout/bottom_sheet_date" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>